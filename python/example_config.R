## Examples for how to parameterize EC data functions
## we will probably move this to a YAML file eventually. 

config <- list(
  # things that can be done for everything listed here
  'default_denom' = 100,
  'denoms' = list('p' = 10, 'l' = 5, 'th' = 30, 'dw' = 10),
  # we can insert these in every place where they occur, just name them appropriately ------
  'standard_pl_value' = 9999,
  'standard_other_value' = 2419.6,
  'standard_other_other_value' = 200
  
)